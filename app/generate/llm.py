def generate(prompt: str) -> str:
    """Minimal deterministic generator placeholder.

    Replace with actual LLM call (OpenAI, local LLM, etc.).
    """
    return "[generated answer] " + (prompt[:500] if prompt else "")
